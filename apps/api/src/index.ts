/**
 * Camada: entrada de pacote.
 *
 * Este módulo declara explicitamente que a API é iniciada pelo processo em
 * server.ts; ele evita exportar composição interna como superfície pública.
 */
export {};
